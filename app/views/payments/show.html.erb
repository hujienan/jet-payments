  <h4>Congratulations <span class="text-primary text-capitalize"><%= @payment.name_on_card %></span></h4>
  <p>Your payment has been done successfully! Please check your payment details below:</p>
  <div class="row justify-content-center border-top border-bottom mb-4">
    <div class="p-4">
      <p class="d-table-row"><span class="d-table-cell pr-4 font-weight-bold">ID:</span> <span class="table-cell"><%= @payment.id %></span></p>
      <p class="d-table-row"><span class="d-table-cell pr-4 font-weight-bold">Card Number:</span> <span class="table-cell"><%= @payment.card_number.gsub(/(.{4})(?=.)/, '\1 \2') %></span> </p>
      <p class="d-table-row"><span class="d-table-cell pr-4 font-weight-bold">Email Address:</span> <span class="table-cell"><%= @payment.email_address %></span> </p>
      <p class="d-table-row"><span class="d-table-cell pr-4 font-weight-bold">Billing Address:</span> <span class="table-cell"><%= @payment.billing_address %></span> </p>
      <% if not @payment.description.blank? %>
        <p class="d-table-row"><span class="d-table-cell pr-4 font-weight-bold">Description:</span><%= @payment.description %></p>
      <% end %>
    </div>
  </div>

<%= link_to "Home", root_path %>